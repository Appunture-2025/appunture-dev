name: Copilot Agent Nightly

on:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  run-agent:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      # Node 22 é OBRIGATÓRIO para a versão atual do Copilot CLI
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      # Instala PowerShell (necessário pois o agent detecta pwsh)
      - name: Install PowerShell
        run: |
          sudo apt-get update
          sudo apt-get install -y powershell

      # Instala o CLI do Copilot (método oficial e suportado)
      - name: Install GitHub Copilot CLI
        run: |
          npm install -g @github/copilot
          copilot --version

      # Autentica no GitHub CLI (necessário para PRs e commits)
      - name: Authenticate GitHub CLI
        run: echo "${{ secrets.GITHUB_TOKEN }}" | gh auth login --with-token

      # REMOVIDO: A etapa "Authenticate Copilot Workspace" com --token não é suportada.
      # A autenticação acontece automaticamente no passo "Run Copilot Agent Plan"
      # através da variável de ambiente.

      # Configura identidade do Git
      - name: Configure Git user
        run: |
          git config user.name "copilot-agent"
          git config user.email "copilot-agent@users.noreply.github.com"

      # Executa o Agent Plan
      - name: Run Copilot Agent Plan
        env:
          # O CLI lê esta variável automaticamente para autenticação
          COPILOT_WORKSPACE_TOKEN: ${{ secrets.COPILOT_WORKSPACE_TOKEN }}
          # É boa prática passar o GITHUB_TOKEN também caso o agente precise acessar a API do GH
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          COPILOT_WORKSPACE_BRANCH: automation/copilot-agent
        run: copilot workspace agent --plan automation/agent-plan.md --apply --model gpt-5.1-codex

      # Faz upload dos logs do Copilot CLI
      - name: Upload Copilot logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: copilot-agent-logs
          path: ~/.config/copilot/logs