# =============================================================================
# Backend Java - Environment Variables
# =============================================================================
# Copy this file to `.env` or set these as environment variables in your shell.
# For Cloud Run, these are set via --set-env-vars or Secret Manager.

# =============================================================================
# Firebase Configuration
# =============================================================================
# Your Firebase project ID
FIREBASE_PROJECT_ID=appunture-tcc

# Firebase Storage bucket name
FIREBASE_STORAGE_BUCKET=appunture-tcc.appspot.com

# Enable/disable Firebase (set false for local dev without Firebase)
FIREBASE_ENABLED=true

# Firebase Service Account Key (JSON string, base64 encoded for CI/CD)
# For local dev: Leave empty to use Application Default Credentials (ADC)
# For Cloud Run: Use Secret Manager reference
FIREBASE_SERVICE_ACCOUNT_KEY=

# =============================================================================
# Google AI (Gemini) Configuration
# =============================================================================
# Get your API key from https://makersuite.google.com/app/apikey
GOOGLE_AI_API_KEY=your-gemini-api-key

# =============================================================================
# Thumbnail Generation (Optional)
# =============================================================================
# If using Cloud Functions for thumbnail generation
THUMBNAIL_GENERATION_ENABLED=true
THUMBNAIL_FUNCTION_URL=

# =============================================================================
# Server Configuration
# =============================================================================
# Port to run the server on (Cloud Run sets this automatically)
PORT=8080

# Spring profile (dev, prod)
SPRING_PROFILES_ACTIVE=prod

# =============================================================================
# Security Configuration
# =============================================================================
# CORS allowed origins (comma-separated for production)
# Example: https://appunture.com,https://admin.appunture.com
CORS_ALLOWED_ORIGINS=

# Rate limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_CAPACITY=200
RATE_LIMIT_REFILL_TOKENS=200
